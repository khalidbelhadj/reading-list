// Custom server entry: identical to Start's default, kept as the place a
// startup-time side effect would go.
import defaultEntry from "@tanstack/react-start/server-entry";

export default defaultEntry;
