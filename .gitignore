# dependencies
node_modules/

# vite / tanstack start
dist/
.tanstack/
.nitro/
.output/
.vercel/
# bun-only repo: stray lockfiles from other package managers switch Vercel installs
pnpm-lock.yaml
package-lock.json
yarn.lock
out/

# electron
dist-electron/
release/

# build
*.tsbuildinfo

# env: every profile file holds real secrets. --env=local reads
# .env.local-stack, --env=prod reads .env.prod (see scripts/profiles.ts).
.env
.env.*

# debug
npm-debug.log*

# misc
.DS_Store
*.pem

# drizzle
db/*.db
db/*.db-journal

# claude
.claude/*
!.claude/skills/
!.claude/launch.json

# agents
.agents/
/skills-lock.json
/tmp

.understand-anything

# Local rollback backup from the flashcard→notes migration (contains user notes)
scripts/flashcard-migration-snapshot*.json

# Generated output of the code-complexity analyzer (bun run analyze)
analysis/code/

# Built Chrome extension bundle (run extension/build.sh to regenerate)
reading-list-extension.zip

# Local-only profiling notes (kept out of the repo)
notes/performance-profile-2026-07-02.md

# mac app (SwiftPM output + the assembled dev bundle, see mac/README.md)
mac/.build/
mac/build/
# the hosted markdown editor page, built by `bun run build:editor`
mac/Sources/ReadingList/Resources/Editor/
